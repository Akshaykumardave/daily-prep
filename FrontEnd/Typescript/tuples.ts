/**
 * Typescript tuple is a special kind of array that allows us to
 * store a fixed number of elements and give each element a specific type and order.
 */

let person: [string, number] = ["Alice", 25];
console.log(person);

let details: [string, number?] = ['Male'];
console.log(details);